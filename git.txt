no branch criado localmente test:

git pull origin test
git config --global pull.ff only
git pull origin test
git pull --rebase=merges
git branch --set-upstream-to origin/test
git fetch origin test
git rebase origin/test

---------------------------------- Passo Padrão----
git add -A
git commit -m "creating m07"
git push origin main


---copia de uma branch para outra----

-estando na branch b1
git checkout main

-o comando abaixo pega a versão mais recente do arquivo da branch b2
git checkout test /home/felipe/Documents/DS_em_Producao/ds_em_producao


depois volta ao passo padrão
